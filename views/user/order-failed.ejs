<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Payment Failed</title>

    <style>

/* Page Background */
body {
  font-family: 'Inter', system-ui, sans-serif;
  background: linear-gradient(135deg, #ffe5e5, #fff);
  color: #7f1d1d;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
  min-height: 100vh;
  animation: fadeIn 0.6s ease-out;
}

/* Fade Animation */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* Card Style */
.card {
  background: #ffffff;
  padding: 2.5rem;
  border-radius: 20px;
  box-shadow: 0 12px 30px rgba(220, 38, 38, 0.15);
  max-width: 430px;
  text-align: center;
  transition: 0.3s ease-in-out;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 40px rgba(220, 38, 38, 0.25);
}

/* Icon */
h1 {
  font-size: 2rem;
  color: #dc2626;
  font-weight: 700;
  margin-bottom: 10px;
}

/* Paragraphs */
.card p {
  color: #991b1b;
  font-size: 0.95rem;
}

/* Button */
.btn {
  background: #dc2626;
  color: #fff;
  padding: 12px 24px;
  border-radius: 12px;
  text-decoration: none;
  font-weight: 600;
  display: inline-block;
  margin-top: 20px;
  transition: 0.3s ease;
  box-shadow: 0 6px 15px rgba(220, 38, 38, 0.3);
}

.btn:hover {
  background: #b91c1c;
  box-shadow: 0 10px 20px rgba(220, 38, 38, 0.4);
  transform: translateY(-2px);
}

</style>


    <!-- <style>
      body {
        font-family: system-ui, sans-serif;
        background: #fef2f2;
        color: #991b1b;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        height: 100vh;
      }
      .card {
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        padding: 40px;
        max-width: 400px;
        text-align: center;
      }
      h1 {
        color: #dc2626;
        margin-bottom: 15px;
      }
      .btn {
        background: #dc2626;
        color: #fff;
        padding: 10px 20px;
        border-radius: 8px;
        text-decoration: none;
        display: inline-block;
        margin-top: 20px;
      }
      .btn:hover {
        background: #b91c1c;
      }
    </style> -->
  </head>
  <body>
    <div class="card">
      <h1>❌ Payment Failed</h1>
      <p>It looks like your payment did not go through.</p>
      <p>Please try again or use another payment method.</p>
       <a href="/orders" 
       class="btn btn-success mt-4 px-4 py-2 shadow-sm">
      View My Orders
    </a>
    </div>
  </body>
</html>








<!-- views/user/order-failed.ejs -->
<!-- <!DOCTYPE html>
<html>
  <head>
    <title>Payment Failed</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body class="text-center p-5">
    <h2 style="color: red;">⚠️ Payment Failed</h2>
    <p>Your order (<%= orderId %>) was created, but payment was not successful.</p>
    <p>Total: ₹<%= grandTotal %></p>
    <p>Payment Method: <%= paymentMethod %></p>

    <a href="/user/orders" class="btn btn-primary mt-3">View My Orders</a>
  </body>
</html> -->

